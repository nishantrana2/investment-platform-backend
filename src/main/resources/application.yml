spring:
  application:
    name: investment-platform

  datasource:
    url: jdbc:mysql://localhost:3306/investment_platform
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

server:
  port: 8080
